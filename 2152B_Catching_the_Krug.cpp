#include <bits/stdc++.h> 
using namespace std; 
#define int long long

void fast_io() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
}

signed main() {
    fast_io(); 
    int t;cin>>t;
    while(t--){
        int n,kx,dx,ky,dy;cin>>n>>kx>>dx>>ky>>dy;
        int res=0;
        if(abs(kx-dx)+1<abs(ky-dy)){
            if(abs(dx-kx)>abs(dy-ky)){
                
            }
        }
    }
    return 0;
}