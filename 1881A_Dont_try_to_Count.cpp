#include <iostream> 
#include <vector>   
#include <algorithm> 
using namespace std; 

void fast_io() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);
    std::cout.tie(NULL);
}

int main() {
    fast_io(); 
    int t;cin>>t;
    for(int u=0;u<t;u++){
        int a,b;
        cin>>a,b;
        string c,d;
        cin>>c>>d;
        int result=0;
        while(c.length()<=d.length()){
            for(int y=0;y<c.length()-b+1;y++){
                
            }
        }
    }
    return 0;
}